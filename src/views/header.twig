<!doctype html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport"
          content="width=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>What instagram knows about @{{ Profile.username|default('you') }}?</title>
        <style>
            {{ source('/style/zuck.css') }}
            {{ source('/style/snapgram.css') }}
            {{ source('/style/style.css') }}
        </style>

    <script>
        {{ source('/js/zuck.js') }}
    </script>
</head>
<body>
    <header class="header">
        <h1 class="header__caption">What instagram knows about @{{ Profile.username|default('you') }}? 🤔</h1>
    </header>